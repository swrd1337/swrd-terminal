import styled from "styled-components";

export const BackgroundImage = styled.img`
  filter: blur(12px);
  -webkit-filter: blur(12px);
  position: fixed;
  z-index: -1;
  height: 100%;
  min-width: 100%;
  min-height: 100%;
`;
